# User profiles default loader
# Imports all user-level profile modules
# User profiles provide collections of home-manager packages for specific use cases

{
  imports = [
    ./productivity.nix
  ];
}

