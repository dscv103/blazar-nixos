# Shared constants
# Common values used across system and user configurations
# This provides a single source of truth for shared settings

{
  # ============================================================================
  # USER INFORMATION
  # ============================================================================
  
  users = {
    mainUser = "dscv";
    description = "David Vitrano";
  };

  # ============================================================================
  # SYSTEM SETTINGS
  # ============================================================================
  
  system = {
    stateVersion = "24.11";
    timeZone = "America/New_York";
    locale = "en_US.UTF-8";
  };

  # ============================================================================
  # NETWORK SETTINGS
  # ============================================================================
  
  network = {
    # Network settings can be added here as needed
    # e.g., DNS servers, proxy settings, etc.
  };

  # ============================================================================
  # PATHS
  # ============================================================================
  
  paths = {
    wallpapers = "~/Pictures/Wallpapers";
    screenshots = "~/Pictures/Screenshots";
  };
}

